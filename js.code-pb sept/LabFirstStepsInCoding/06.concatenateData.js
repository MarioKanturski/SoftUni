function concatenateDate (firstName,secondName,old,city) {
    console.log(`You are ${firstName} ${secondName}, a ${old}-years old person from ${city}.`);

}

concatenateDate("Mario","Kanturski",20,"Blagoevgrad");